#!/usr/bin/env bash
# This script displays the PID and process name of processes containing the word "bash."

# Use pgrep to find processes with "bash" in their name and display their PIDs and names
pgrep -a bash | awk '{print $1, $2}'
